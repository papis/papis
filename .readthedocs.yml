version: 2

sphinx:
  configuration: doc/source/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
